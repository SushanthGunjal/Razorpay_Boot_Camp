# A simple math quiz game

## Instructions to execute the program

### go run main.go -csv=filename.csv -limit=xseconds -shuffle=true/false

All the three fields are optional

* csv by default is set to problems.csv.

* limit by default is set to 30 seconds.

* shuffle by default is set to false. 


